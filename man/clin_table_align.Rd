% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col_width.R
\name{clin_table_align}
\alias{clin_table_align}
\title{Set how a table sits across the page}
\usage{
clin_table_align(x, align)
}
\arguments{
\item{x}{A clintable object}

\item{align}{One of "left", "center", or "right"}
}
\value{
A clintable object
}
\description{
flextable centres a table on the page. Regulatory outputs are usually
flush left, and a narrow table sometimes wants to be centred deliberately.
The choice is recorded on the clintable and applied when the table renders,
after the default styling function has run, so it holds even when an
organisation's own \code{clinify_table_default()} rebuilds the table properties.
}
\details{
This is only about where the table sits across the page. How wide it is, and
how that width is divided between the columns, is \code{\link[=clin_col_widths]{clin_col_widths()}}.
}
\examples{
clintable(mtcars) |>
  clin_table_align("left")
}
